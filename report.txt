Отчет за неделю:
1) Выбрано решение - протокольный формат передачи данных черех байт-код(не через сериализацию),
    пока не доработал окончательное решение
2) В common добавлены:
    enum Command - название команд и байт код операции
    CommandUtility - модуль по работе с  Command
    enum JobStage - каждая команда содержит последовательность действий, последовательность храниться
    в этом перечислении
3) В cloud_server добавлены:
   AuthServer - специальный класс, отвечающий за авторизацию, хранит подключение к БД с логинами,
   пока в качестве заглушки используется Map
   NetworkServerNetty - класс, отвечающий за сетевые операции
   ClientHandlerNetty - хендлер для обработки входящих/исходящих сообщений, разработан кусок авторизации
4) В cloud_client добавлены:
    Client - JavaFX приложение
    clientForm.fxml - в папку resources добавлен файл формы - добавлены поля для авторизации,
    планируется добавить возможность сохранять настройки
    ClientController - контроллер для формы
    Network - отвечает за сетевые действия - основной модуль, будет работать на NIO (?)

Вопросы:
    хочу старые файлы с io/nio не удалять,а помещать в отдельный подпакет old, но ругается,
    т.к. есть ссылки на FileUtility модуля common. Например, хочу поместить в cloud_server ClientHandler
    в подпапку old - ругается на FileUtility.


